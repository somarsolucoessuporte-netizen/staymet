// Tailwind 4 — configuration is primarily in globals.css via @theme
// This file is kept minimal for compatibility
import type { Config } from 'tailwindcss'

const config = {} satisfies Config

export default config
